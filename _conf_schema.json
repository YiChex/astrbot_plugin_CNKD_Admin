{
  "group_whitelist": {
    "description": "监控的白名单群聊UMO列表",
    "type": "list",
    "default": ["QQ:GroupMessage:1030157691"],
    "hint": "每个群聊的UMO格式为：QQ:GroupMessage:群号"
  },
  "admin_qq_list": {
    "description": "接收敏感词提醒的管理员QQ列表",
    "type": "list", 
    "default": ["QQ:FriendMessage:475407353"],
    "hint": "管理员UMO列表，格式：QQ:FriendMessage:QQ号"
  },
  "api_endpoint": {
    "description": "敏感词检测API端点",
    "type": "string",
    "default": "https://uapis.cn/api/v1/text/profanitycheck",
    "hint": "敏感词检测API地址"
  },
  "api_rate_limit": {
    "description": "API调用频率限制",
    "type": "object",
    "hint": "限制API调用频率，避免超过限制",
    "items": {
      "max_calls_per_minute": {
        "description": "每分钟最大调用次数",
        "type": "int",
        "default": 60,
        "hint": "避免API调用过于频繁，建议根据API提供商限制设置"
      },
      "max_calls_per_hour": {
        "description": "每小时最大调用次数",
        "type": "int",
        "default": 1000,
        "hint": "小时级限制，防止突发大量调用"
      }
    }
  },
  "cache_config": {
    "description": "消息缓存配置",
    "type": "object",
    "hint": "配置消息内容缓存，减少重复API调用",
    "items": {
      "cache_ttl": {
        "description": "缓存有效期（秒）",
        "type": "int",
        "default": 3600,
        "hint": "相同内容在多长时间内不再重复检测"
      },
      "max_cache_size": {
        "description": "最大缓存条目数",
        "type": "int",
        "default": 10000,
        "hint": "缓存的最大消息条目数"
      }
    }
  },
  "retry_config": {
    "description": "重试策略配置",
    "type": "object",
    "hint": "API调用失败时的重试策略",
    "items": {
      "max_retries": {
        "description": "最大重试次数",
        "type": "int",
        "default": 3,
        "hint": "API调用失败时重试的次数"
      },
      "base_delay": {
        "description": "基础延迟时间（秒）",
        "type": "float",
        "default": 1.0,
        "hint": "第一次重试前的等待时间"
      },
      "max_delay": {
        "description": "最大延迟时间（秒）",
        "type": "float",
        "default": 30.0,
        "hint": "重试之间的最大等待时间"
      }
    }
  },
  "group_notice_enabled": {
    "description": "是否在群内发送敏感词提醒",
    "type": "bool",
    "default": true,
    "hint": "开启后，检测到敏感词会在群内发送提醒消息"
  },
  "notice_template": {
    "description": "敏感词提醒模板",
    "type": "text",
    "default": "检测到敏感词，消息已撤回并禁言处理。\n检测到的敏感词：{forbidden_words}\n违规次数：第{violation_count}次",
    "hint": "可用变量：{forbidden_words}, {original_text}, {masked_text}, {violation_count}"
  },
  "admin_notice_template": {
    "description": "管理员私聊提醒模板", 
    "type": "text",
    "default": "🚨 敏感词警报\n群聊：{group_id}\n用户：{user_name} ({user_id})\n违规次数：第{violation_count}次\n敏感词：{forbidden_words}\n原文：{original_text}\n处理：禁言{ban_duration}秒，消息已撤回\n时间：{timestamp}",
    "hint": "可用变量：{group_id}, {user_name}, {user_id}, {forbidden_words}, {original_text}, {violation_count}, {ban_duration}, {timestamp}"
  },
  "statistics_enabled": {
    "description": "启用统计功能",
    "type": "bool",
    "default": true,
    "hint": "开启后会统计每个群的敏感词检测数据"
  },
  "cooldown_seconds": {
    "description": "同一用户冷却时间（秒）",
    "type": "int",
    "default": 60,
    "hint": "同一用户连续发言的检测冷却时间"
  },
  "enable_auto_ban": {
    "description": "启用自动禁言",
    "type": "bool",
    "default": true,
    "hint": "开启后，检测到敏感词会自动禁言用户"
  },
  "ban_rules": {
    "description": "禁言规则配置",
    "type": "object",
    "hint": "阶梯式禁言规则设置",
    "items": {
      "first_ban_duration": {
        "description": "首次违规禁言时长（秒）",
        "type": "int",
        "default": 60,
        "hint": "第一次触发敏感词的禁言时长"
      },
      "second_ban_duration": {
        "description": "第二次违规禁言时长（秒）",
        "type": "int",
        "default": 600,
        "hint": "同一天内第二次触发敏感词的禁言时长"
      },
      "third_ban_duration": {
        "description": "第三次违规禁言时长（秒）",
        "type": "int",
        "default": 86400,
        "hint": "同一天内第三次触发敏感词的禁言时长（24小时）"
      },
      "reset_time": {
        "description": "违规次数重置时间（小时）",
        "type": "int",
        "default": 4,
        "hint": "每天凌晨几点重置违规次数（24小时制）"
      }
    }
  },
  "exempt_roles": {
    "description": "免禁言角色",
    "type": "list",
    "default": ["owner", "admin"],
    "hint": "拥有这些群身份的用户不会触发自动禁言"
  },
  "violation_log_enabled": {
    "description": "启用违规日志记录",
    "type": "bool",
    "default": true,
    "hint": "开启后会记录详细的违规日志"
  },
  "max_log_days": {
    "description": "日志保留天数",
    "type": "int",
    "default": 30,
    "hint": "违规日志保留的最大天数"
  },
  "enable_message_delete": {
    "description": "启用消息撤回",
    "type": "bool",
    "default": true,
    "hint": "检测到敏感词时自动撤回原消息"
  },
  "bypass_rate_limit": {
    "description": "绕过限流策略",
    "type": "bool",
    "default": true,
    "hint": "启用后插件将尝试绕过AstrBot的限流策略"
  },
  "custom_forbidden_words": {
    "description": "自定义违禁词列表",
    "type": "list",
    "default": ["测试敏感词1", "测试敏感词2"],
    "hint": "本地自定义的违禁词，会在API检测之外额外检查"
  },
  "enable_local_check": {
    "description": "启用本地关键词检测",
    "type": "bool",
    "default": true,
    "hint": "在调用API前先进行本地关键词检测"
  },
  "debug_mode": {
    "description": "调试模式",
    "type": "bool",
    "default": false,
    "hint": "开启调试日志输出"
  }
}
